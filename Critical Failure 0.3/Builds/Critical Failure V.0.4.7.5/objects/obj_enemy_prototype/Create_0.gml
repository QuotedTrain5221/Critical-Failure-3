// Enemy base data-Create Even

enemy_name = "Prototype";
enemy_zone = 2;// Starting zone for camera
target_zone = enemy_zone;

move_timer = 0;
move_delay_min = 180;  // 3 seconds at 60fps
move_delay_max = 360;  // 6 seconds

global.is_watched = false;
