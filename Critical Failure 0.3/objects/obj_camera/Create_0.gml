camera = camera_create_view(0,0,1920,1080,0,obj_player,0,0,0,0);//creates the cameras view as well as its variables for how much of an area is being covered. 
view_camera[0]=camera;// This activates the camera within the room.
view_visible[0] = true;// This makes the players perspective be the camera view of viewport 0.

